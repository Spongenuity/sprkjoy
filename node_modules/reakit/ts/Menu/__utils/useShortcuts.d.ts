import * as React from "react";
import { MenuStateReturn } from "../MenuState";
export declare function useShortcuts({ stops, move }: Pick<MenuStateReturn, "stops" | "move">, timeout?: number): (event: React.KeyboardEvent<Element>) => void;
